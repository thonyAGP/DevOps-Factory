name: Coverage Audit

on:
  schedule:
    - cron: '0 6 * * 1' # Monday 6am UTC
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  coverage-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - run: pnpm install --frozen-lockfile

      - name: Run coverage baseline scanner
        run: pnpm coverage
        env:
          GH_TOKEN: ${{ secrets.FACTORY_PAT }}

      - name: Commit coverage data
        run: |
          git config user.name "devops-factory[bot]"
          git config user.email "devops-factory[bot]@users.noreply.github.com"
          git add data/coverage-history.json
          git diff --cached --quiet || git commit -m "data: update coverage baseline"
          git pull --rebase
          git push
